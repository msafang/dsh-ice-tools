import type { HostContext } from '../../core/dsh-adapter/index.ts'

export const name = 'ice-session-id'
export const stubOnly = true
export const descriptionKey = 'modules.sessionId.description'

export function apply(ctx: HostContext): void {
  // Phase X: real implementation.
  void ctx
}
